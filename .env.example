# Database connection string for Docker Compose
# Use service name 'dietcoach-db' for Docker networking
DATABASE_URL="postgresql://dietcoach:supersecret@dietcoach-db:5432/dietcoach?connection_limit=10&pool_timeout=20"

# API server port
PORT=3000

# Node.js environment
NODE_ENV=production
